[project]
name = "visual-studio"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "pillow>=12.2.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pytest-codspeed>=5.0.3",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["benchmarks"]
